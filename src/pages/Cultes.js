import React from "react";

const Cultes = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Culte
      {console.log("JE suis dans Cultes")}
    </div>
  );
};

export default Cultes;
